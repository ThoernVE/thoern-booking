import { Container, Row, Col } from "react-bootstrap"

export default function AvailableTimes() {
    return (
        <Container>
            <Row>
                <Col>
                    <h1>This is where we can see our own available times</h1>
                </Col>
            </Row>
        </Container>
    )
}